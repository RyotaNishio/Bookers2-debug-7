<%= form_with model:[book, book_comment], local:false do |f| %>
  <div id="comments-error" class="text-danger">
    <%= render 'layouts/errors', obj: book_comment %>
  </div>
  <%= f.text_area :comment, rows:'5', class: "form-control" %>
  <%= f.submit "送信" %>
<% end %>
